import { AngularOptionalType } from "../../angular/types/types";
import { NOT_SUPPORTED_TYPE } from "../../morph";
import { NextjsTypeNames } from "../types/nextjs-types";

export function angularFilesToAffect(filePathWithName: string, fileTree: string[], type: NextjsTypeNames, optionalTypes: AngularOptionalType[]): string[] | NOT_SUPPORTED_TYPE {
    switch(type) {
    }
}